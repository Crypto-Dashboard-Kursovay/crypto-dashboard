
# План курсовой 

## Введение 

- Актуальность автоматизации торговли на криптовалютных биржах.
- Проблема: высокий порог входа для использования готовых ботов, отсутствие прозрачности и гибкости.
- Цель: разработать собственную событийно-ориентированную систему алгоритмической торговли с веб-интерфейсом, способную работать в тестовых средах (песочницах) бирж Binance и Bybit.
- Задачи:
	1. Проанализировать предметную область, принципы спотовой торговли и существующие алгоритмические стратегии.
	2. Спроектировать архитектуру системы, объединяющую торговое ядро, сервер API и клиентский веб-интерфейс.
	3. Реализовать торговый движок с подключаемыми адаптерами бирж и набором базовых стратегий.
	4. Разработать веб-панель управления для конфигурации стратегий, мониторинга позиций и визуализации сделок.
	5. Провести тестирование системы в песочницах Binance Testnet и Bybit Testnet, а также выполнить бэктестинг на исторических данных.
- Объект исследования: процесс алгоритмической торговли криптоактивами.
- Предмет исследования: архитектура, методы и инструменты построения автоматизированной торговой системы с веб-управлением.

## Глава 1. Анализ предметной области и постановка задачи 

- 1.1. Основы спотовой торговли на криптобиржах: ордера, стакан, комиссии, ликвидность.
- 1.2. Обзор алгоритмических стратегий для спотового рынка:
    - Трендовые стратегии (SMA Cross, MACD).
    - Стратегии возврата к среднему (RSI, полосы Боллинджера).
    - Выбор базовых стратегий для реализации (обоснование простоты и наглядности).
- 1.3. Обзор инструментов для создания торговых систем:
    - Готовые open-source боты (Freqtrade, Hummingbot) — их достоинства и ограничения.
    - Языки и библиотеки: Python с `ccxt`, `FastAPI`, `pandas`; React для пользовательского интерфейса.
    - Тестовые среды бирж (Binance Testnet, Bybit Testnet) как безопасный полигон для отладки.
- 1.4. Анализ требований к разрабатываемой системе:    
    - Функциональные требования: запуск/остановка стратегий, работа с несколькими биржами и парами, real-time мониторинг, бэктестинг.
    - Нефункциональные требования: отказоустойчивость, расширяемость, контейнеризация, удобный UI.
- Выводы по главе и уточнённая постановка задачи.

#### Глава 2. Проектирование архитектуры системы

- 2.1. Общая архитектура: событийно-ориентированный подход, разделение на независимые компоненты.
- 2.2. Компонентная схема и взаимодействие:
    - **Trading Engine** — ядро, реализующее стратегии и управляющее ордерами.
    - **Backend API** (FastAPI) — REST и WebSocket-интерфейс для фронтенда.
    - **Frontend** (React SPA) — дашборд и панель управления.
    - **Слой хранения** — PostgreSQL (конфигурации, история сделок) и Redis (кэш состояния, Pub/Sub).
- 2.3. Детальное проектирование Trading Engine:
    - Паттерн «Чистая архитектура»: Domain (сущности Strategy, Signal, Order), Application (StrategyRunner, OrderExecutor, RiskManager), Infrastructure (ExchangeAdapter, MarketDataProvider).
    - Полиморфизм адаптеров бирж: `BinanceTestnetAdapter`, `BybitTestnetAdapter`.
    - Поток событий: получение свечей, расчёт индикаторов, генерация сигналов, проверка рисков, исполнение ордера, публикация результата.
- 2.4. Проектирование Backend API и взаимодействия в реальном времени:
    - REST-методы для CRUD стратегий и получения истории.
    - WebSocket-канал `/ws/updates`: подписка на события движка через Redis Pub/Sub, push-уведомления клиенту.
- 2.5. Проектирование Frontend:
    - Структура страниц: Dashboard, Управление стратегиями, История сделок, Бэктестинг.
    - Компоненты визуализации: графики курса и индикаторов, таблица ордеров, кривая доходности.
- 2.6. Модуль бэктестинга:
    - Использование общего кода стратегий с подменой источника данных на исторический и симуляцией исполнения (PaperExchangeAdapter).
    - Хранение и отображение метрик (Profit Factor, Max Drawdown, Sharpe Ratio).
- 2.7. Выбор технологического стека: Python 3.11, FastAPI, SQLAlchemy, Alembic, `ccxt` + `ccxt.pro`, React 18, TypeScript, Docker Compose, PostgreSQL, Redis.

#### Глава 3. Реализация системы

- 3.1. Настройка окружения и контейнеризация: описание Docker Compose файлов и переменных окружения.
- 3.2. Реализация инфраструктурного слоя:
    - Адаптеры бирж: методы `fetch_ohlcv`, `create_order` для Binance и Bybit Testnet.
    - Провайдер рыночных данных через WebSocket (`ccxt.pro`), восстановление соединения.
    - Репозитории для работы с PostgreSQL (модели стратегий, ордеров).
    - Клиент Redis для кэширования и Pub/Sub.
- 3.3. Реализация доменного слоя и Application-логики:
    - Классы стратегий (SMA Cross, RSI) — код и пояснение логики генерации сигналов.
    - RiskManager: контроль минимального лота, максимальной позиции, стоп-лосса.
    - StrategyRunner: асинхронный цикл обработки свечей.
    - OrderExecutor: преобразование сигнала в лимитный/рыночный ордер с обработкой ответа биржи.
- 3.4. Реализация Backend API:
    - Маршруты `/api/strategies`, `/api/account`, `/api/trades`, `/api/backtest`.
    - WebSocket endpoint и трансляция событий из Redis в клиентские подключения.
    - Генерация документации через Swagger UI.
- 3.5. Реализация Frontend:
    - Структура React-приложения (компоненты, хуки, контекст).
    - Real-time обновления через WebSocket (кастомный хук `useWebSocket`).
    - Визуализация графиков (свечной график, наложение индикаторов) с помощью библиотеки Lightweight Charts.
- 3.6. Реализация модуля бэктестинга:
    - Загрузка исторических данных из файлов (CSV/Parquet) или через REST API бирж.
    - Имитация исполнения ордеров с учётом комиссий и проскальзывания.
    - Расчёт метрик и формирование отчёта.

#### Глава 4. Тестирование и анализ результатов

- 4.1. Сценарии и параметры тестирования:
    - Пары: BTC/USDT, ETH/USDT.
    - Таймфреймы: 5m, 1h.
    - Стратегии: SMA(50,200), RSI(14) с уровнями 30/70.
- 4.2. Тестирование в песочнице Binance Testnet:
    - Запуск стратегий на протяжении заданного периода, логирование сделок.
    - Верификация поведения: соответствие сигналов индикаторам, корректность исполнения ордеров.
- 4.3. Тестирование в песочнице Bybit Testnet:
    - Аналогичный запуск для демонстрации работы адаптера другой биржи.
    - Сравнение показателей комиссий и скорости исполнения (обзорно).
- 4.4. Бэктестинг стратегий на исторических данных:
    - Результаты моделирования за 6–12 месяцев.
    - Ключевые метрики: Profit Factor, Sharpe Ratio, Maximum Drawdown, общая доходность, процент прибыльных сделок.
    - Сравнение эффективности стратегий в разных рыночных условиях.
- 4.5. Оценка пользовательского интерфейса: удобство мониторинга и настройки, юзабилити-выводы.

- 4.6. ML-расширение: модель предсказания направления цены на BTC/USDT (H1).

    - **4.6.1. Постановка задачи.** Цель эксперимента — проверить, может ли модель машинного обучения, обученная на признаках, производных от индикатора RSI, обходить базовую пороговую стратегию `RsiThreshold(14, 30/70)` (реализована в `trade-engine-crypto/src/strategies/rsi_threshold.py`) на спотовом BTC/USDT. Инструмент — BTC/USDT на Binance Spot, таймфрейм — H1: на нём суточный шум уже сглажен, а одного календарного месяца достаточно для регулярного дообучения (~720 баров). Источник данных — те же исторические OHLCV-свечи, которые движок использует для обычного бэктеста: их подгружает `CcxtHistoricalMarketDataProvider` (`infrastructure/ccxt_historical.py`) с кешированием в CSV через `CSVMarketDataProvider` (`infrastructure/csv_market_data.py`).

    - **4.6.2. Признаки и целевая переменная.** Базовый признак — RSI(14) по Wilder (используется та же формула с EMA-сглаживанием `alpha = 1/N`, что и в `RsiThreshold.on_candle`), плюс производные: `rsi_diff_1` и `rsi_diff_3` (скорость и импульс RSI), `rsi_zscore_24` (z-нормированное значение по окну в сутки = 24 свечи на H1), а также `dist_oversold = rsi − 30` и `dist_overbought = 70 − rsi` — расстояния до классических порогов, превращающие пороговую логику в непрерывный признак. Целевая переменная — бинарная: `y_t = 1`, если `close(t + H) > close(t) · (1 + 2·fee)`, и `0` иначе. Горизонт `H = 6` баров (~6 часов) выбран как компромисс между шумом одиночной свечи и затуханием сигнала. Множитель `2·fee` гарантирует, что положительная метка соответствует прибыли после round-trip комиссий (≈0.2% на Binance Spot).

    - **4.6.3. Модель и схема обучения.** В качестве модели выбран градиентный бустинг на деревьях (`LightGBM` или `sklearn.GradientBoostingClassifier`) — он подходит для табличных признаков, устойчив к выбросам и не требует масштабирования. Используется схема walk-forward с ежемесячным переобучением: на каждый календарный месяц `M` берётся окно обучения = предыдущие 12 месяцев свечей, признаки и `y` считаются с временным разрывом, исключающим утечку будущей информации, модель валидируется через `TimeSeriesSplit(n_splits=5)` и затем применяется к свечам месяца `M` для генерации торговых сигналов. Решающее правило простое: BUY при `p̂ = P(y=1∣x) > 0.55` и отсутствии открытой позиции, SELL при `p̂ < 0.45` или срабатывании стоп-лосса/тейк-профита — последнее контролирует уже существующий `RiskManager` (`application/risk_manager.py`), который ML-стратегия не подменяет. Интеграция в движок выполняется без правок ядра: модель оформляется как новая реализация интерфейса `Strategy` из `domain/interfaces.py` (метод `on_candle(candle) -> Signal | None`), загрузка веса для текущего месяца происходит в `on_start()`. Благодаря этому ML-стратегия проходит через тот же `BacktestRunner`, что и `RsiThreshold`, — а значит, оценивается на тех же метриках и в идентичных условиях исполнения.

    - **4.6.4. Критерии оценки.** Используются метрики, уже реализованные в `BacktestRunner._build_result` и хранящиеся в `BacktestResult` (`domain/backtest_result.py`): `total_return_pct` — общая доходность за период, `max_drawdown_pct` — максимальная просадка по equity-кривой, `sharpe_ratio` — годовой коэффициент Шарпа (для H1 рассчитывается с числом баров в году `24·365`, см. таблицу `_PERIODS_PER_YEAR` в `backtest_runner.py`), `win_rate` — доля прибыльных пар BUY→SELL и `profit_factor` = Σ прибыли / Σ убытков. Дополнительно отдельно от торговых метрик логируются классификационные метрики модели на out-of-sample фолдах: ROC-AUC, precision и recall на классе «1». Это необходимо, чтобы отличать улучшение торговых показателей за счёт реальной предсказательной силы модели от случайного попадания в благоприятный период.

    - **4.6.5. Визуализация результатов.** По итогам прогона на основе `BacktestResult` (передаётся из движка во фронтенд через тот же WebSocket-канал `/ws/updates`, что и live-обновления) строится двухпанельный график на Lightweight Charts. Верхняя панель — кривая капитала по `equity_curve[]`: в её левом верхнем углу выводится сводка ключевых метрик (Total Return, Sharpe, Max Drawdown, Win Rate, Profit Factor), а на оси времени вертикальными пунктирами отмечены границы месяцев — точки переобучения модели, что позволяет визуально оценить стабильность поведения от окна к окну. Нижняя панель — свечной график BTC/USDT 1h за тот же период с маркерами сделок из `trades[]`: зелёные треугольники вверх — BUY, красные треугольники вниз — SELL; во всплывающей подсказке у каждого маркера выводится вероятность модели `p̂` (из `Signal.metadata`) и PnL закрытой пары для SELL (поле `BacktestTrade.pnl` уже заполняется в `_on_new_trade`). Такая компоновка — «метрики + equity сверху, цена + точки входа снизу» — наглядно показывает связь решений модели с состоянием рынка: видно, не покупает ли модель «на пиках», удерживает ли позицию через локальные просадки и как ведёт себя в трендовых vs. боковых режимах.

    - **4.6.6. Ожидаемый результат и риски.** Базовая гипотеза: добавление контекстных RSI-признаков и нелинейной модели даст прирост Sharpe Ratio относительно эталонной `RsiThreshold(14, 30/70)` при сопоставимой или меньшей максимальной просадке. Основной риск — переобучение на свойствах конкретного режима рынка (бычий 2020–2021, затяжной даун 2022, восстановление 2023–2024); ежемесячный walk-forward и валидация на каждом месяце как out-of-sample именно для этого и нужны. Второй риск — несоответствие комиссий и проскальзывания реальным условиям; в `SimulatedExchangeAdapter` они задаются параметрически, и в отчётности используются значения, соответствующие тарифу Binance Spot (0.1% maker/taker).

#### Заключение

- Итоги выполнения поставленных задач.
- Основные результаты: разработана полнофункциональная система с микросервисной архитектурой, подтверждена её работоспособность на тестовых средах двух бирж.
- Достоинства подхода: расширяемость за счёт адаптеров, единый код стратегий для live и бэктеста.
- Направления дальнейшего развития: расширение ML-модуля (LSTM/Transformer-модели, мультифакторные признаки — orderbook, on-chain, ансамбли), подключение других бирж (OKX, KuCoin), внедрение уведомлений (Telegram), деплой в облако.

#### Список литературы

- (Будет содержать ссылки на документацию `ccxt`, FastAPI, React, научные статьи по алгоритмическим стратегиям, книги по архитектуре ПО и т.д.)

---
![[стэк и архитектура проекта курсовой по созданию бота 07.05.26]]

---
### 📈 Как это подать комиссии: акценты

1. **Clean Architecture + DDD** – покажи диаграмму слоев, объясни, что бизнес-логика не зависит от биржи и UI.
2. **Event-driven** – асинхронная связь через Redis Pub/Sub, WebSocket для real-time обновлений.
3. **Полиморфизм бирж** – благодаря паттерну «Адаптер» легко добавлять новые биржи.
4. **Единый код стратегий** – одни и те же классы используются и в live, и в бэктесте.
5. **Контейнеризация** – Docker Compose для быстрого развертывания.

---
Торговые боты Bybit 

- Фьючерсный grid-бот 
- Фьючерсный мартингейл 
- Спотовый grid-бот
- DCA 

Хочу повторить этих ботов в своей панели, распространяя на разные крипто-биржи через библиотеку ccxt 

---
Разделение обязанностей 

1. Антон фронт (описать фронт, сделать макет, как я его вижу)
2. Я бэкенд (подключение к биржам, логика ботов)

Доп задачи: 

1. Купить домен 

___
Возможности развития курсовой: в порядке сложности 

1. Сделать UI, подключить freqtrade и сделать торговлю в песочнице, рассказав, как это всё работает 
2. Реализовать всё то же самое, только сделать вообще своего торгового бота. 
3. Реализовать базовые алгоритмические торговые подходы + ML подходы 

---
Взять за идею проект Syntra

- **Domain-Driven Design**. Четкое разделение на слои Domain, Application, Infrastructure. Бизнес-логика ядро системы. 
- Управление рисками. Circuit breaker. 

Взять и другие идеи: 
1. Защита от «мести рынку» (winter mode)

На что обратить внимание: 
1. **Freqtrade** Ведущий open-source бот для криптотрейдинга
2. Kairos Cобытийно-ориентированный движок для создания и тестирования стратегий
